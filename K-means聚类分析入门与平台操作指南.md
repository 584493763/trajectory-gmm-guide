# K-means 聚类分析入门与平台操作指南

> 面向医学生与临床研究者的从零到一实操手册
> 适用工具：风暴统计平台 · K-means 聚类分析模块（v4.0.5）
> 底层引擎：R `ClusterR` 包（`KMeans_rcpp` / `Optimal_Clusters_KMeans`）+ `ggplot2` / `ggsci`

---

## 目录

- [第零章 平台入口与数据准备](#第零章-平台入口与数据准备)
- [第一章 认识聚类分析与 K-means](#第一章-认识聚类分析与-k-means)
- [第二章 K-means 统计学理论基础](#第二章-k-means-统计学理论基础)
- [第三章 平台 K-means 模块操作指南](#第三章-平台-k-means-模块操作指南)
- [第四章 完整实战案例](#第四章-完整实战案例)
- [第五章 结果解读与论文写作](#第五章-结果解读与论文写作)
- [第六章 常见问题 FAQ](#第六章-常见问题-faq)
- [附录 A 参数速查表](#附录-a-参数速查表)
- [附录 B 术语表](#附录-b-术语表)
- [附录 C 推荐阅读与参考文献](#附录-c-推荐阅读与参考文献)

---

## 第零章 平台入口与数据准备

### 0.1 平台入口

- **地址**：<http://39.170.49.93:3838/zstats_input/zstats_input_4.0.5/>
- **需要登录**：未登录时页面会弹出"需要登录"遮罩，登录后即可使用全部模块。
- **菜单结构**（左侧导航）：

| 一级 | 二级 |
| --- | --- |
| 本平台介绍 | — |
| 数据导入 | 数据导入、数据集格式介绍 |
| 数据插补 | 回归插补、简单插补、数据缺失情况 |
| 数据整理 | 定量变量转分类、衍生新变量、变量设置、多变量转换、异常值处理、分类变量设置、分析集、共病处理、计算累积暴露 等 |
| **K-means 聚类分析模块** | **最优聚类选择、聚类分析、聚类可视化、分组差异性分析、定量数据差异性分析、数据下载** |
| 本软件教程 | — |

> 本指南只讲 **K-means 聚类分析模块**。若你的数据还没导入，请先走一遍"数据导入"。

### 0.2 数据导入：把数据准备好

平台的数据导入支持 **csv、excel、rds、SPSS（.sav）、SAS** 等格式，单个文件不超过 **10 MB**。平台对数据集的格式要求（摘自平台"数据集格式介绍"）：

1. **首行是变量名**，建议用英文或拼音，**不建议用汉字**，且不要出现 `(`、`*`、`%` 等符号；
2. 从第二行开始，**每一行是一个研究对象**的全部变量信息；
3. **每一列是一个变量**的取值；
4. 变量值建议**用数字表示**（如男性=1、女性=2；血型 1/2/3/4），这样在各统计软件中通行无阻；
5. **缺失值用空白表示，不要写 `NA`**；
6. 除非特殊字符串（如姓名），变量值**不要出现汉字或英文字符串**，否则后续分析容易被卡住。

平台提供了 **CHARLS 示例数据**与 **NHANES 示例数据**，可以先用示例数据把流程走通。

### 0.3 聚类分析对数据的特别要求

相比普通统计分析，K-means 对数据还有几条"硬要求"：

| 要求 | 原因 | 平台里的对应操作 |
| --- | --- | --- |
| **只用数值型变量** | K-means 靠"距离"计算相似度，分类变量（文字）没有数值距离 | 先在"数据整理 → 定量变量转分类 / 分类变量设置"里处理 |
| **不能有缺失值** | 距离无法计算 | 平台"数据插补"模块；或分析前删除缺失 |
| **各变量量纲要可比** | 否则数值大的变量会主导距离 | 模块内提供"**标准化聚类变量**"选项 |
| **变量方向要一致** | 一个"越大越好"、一个"越大越差"会让聚类结果难以解释 | 分析前对反向指标做转换（如用 1/x 或取负） |
| **异常值要处理** | 极端值会"抢走"一个簇 | 平台"数据整理 → 异常值处理" |

> **关于标准化**：本模块的"标准化聚类变量"就是把每个变量转成 z 分数（均值 0、标准差 1），使各变量在距离计算中权重相等。**只要各变量量纲不同（如年龄与身高、血糖与血压），就应当勾选。**

---

## 第一章 认识聚类分析与 K-means

### 1.1 什么是聚类分析

聚类分析（Cluster Analysis）是一类**无监督学习**方法：**在没有事先分组、没有结局变量的情况下**，根据对象之间的"相似程度"自动把它们分成若干组（簇），使得：

- **簇内**的对象尽可能相似；
- **簇间**的对象尽可能不同。

它回答的不是"X 是否影响 Y"，而是——**"这些人/这些区域/这些指标，天然可以分成哪几类？"**

### 1.2 K-means 能解决什么临床问题

| 研究场景 | 聚类变量举例 | 得到的"类"是什么 |
| --- | --- | --- |
| 代谢表型分型 | TyG 指数（甘油三酯-葡萄糖指数）、BMI、腰围、血压 | 不同代谢风险表型 |
| 共病模式 | 高血压、糖尿病、冠心病、卒中（0/1 变量） | 共病组合模式 |
| 患者分型 | 症状评分、生化指标、影像参数 | 疾病亚型 |
| 生活方式 | 吸烟量、饮酒量、体力活动、睡眠时长 | 生活方式类型 |
| 地区/医院分层 | 经济、医疗资源、人口结构指标 | 区域类型 |
| 纵向两次测量 | 基线值 + 随访值（如本指南案例的 TyG_2011 与 TyG_2015） | 水平与变化模式 |

**一句话**：K-means 帮你把"看起来杂乱"的个体，归纳成**少数几个可命名的亚群**，为后续分型、风险分层与精准干预提供依据。

### 1.3 聚类方法家族：为什么这里用 K-means

| 方法 | 核心思路 | 优点 | 局限 |
| --- | --- | --- | --- |
| **K-means** | 最小化簇内平方和，迭代更新质心 | 快、易解释、适合大样本数值数据 | 需预先指定 K；对量纲、异常值敏感；假设簇近似球形 |
| 层次聚类 | 自底向上合并 | 不需要预设 K；可出树状图 | 大样本慢；一次合并不可撤销 |
| K-medoids（PAM） | 用真实样本点作中心 | 对异常值更稳健 | 计算更慢 |
| 高斯混合模型（GMM） | 假设数据来自若干正态分布的混合 | 给出归属概率；可处理椭圆形簇 | 参数多、易不收敛 |
| 潜在类别分析（LCA） | 面向**分类**指标的概率模型 | 适合 0/1、等级变量 | 需专门软件与较大样本 |

本模块采用的是 **K-means**（底层为 `ClusterR::KMeans_rcpp`，基于 Rcpp 实现，速度快、可多起点重复）。

### 1.4 聚类分析 vs 轨迹模型（本仓库另一份指南）

| 维度 | 聚类分析（K-means） | 轨迹模型（GMM/GBTM） |
| --- | --- | --- |
| 数据形态 | **横截面**：每个对象一行 | **纵向长格式**：每个对象多个时间点 |
| 分组含义 | 按"相似程度"分型 | 按"随时间变化的形状"分组 |
| 是否含时间 | 一般不含（或仅把两次测量当作两个变量） | 核心就是时间 |
| 典型输出 | 簇散点图、簇均值表 | 多条轨迹曲线 |
| 本仓库对应指南 | **本文档** | [轨迹模型 GMM 入门与 Shiny 操作指南](./轨迹模型GMM入门与Shiny操作指南.md) |

> 本指南第四章的案例把 2011 与 2015 两次 TyG 测量当作两个变量来聚类——得到的类别可以理解为"**水平 + 变化方向**"的组合，但严格来说仍属于横截面聚类。若研究目标就是"变化轨迹"，请使用轨迹模型模块。

---

## 第二章 K-means 统计学理论基础

本章解释平台上每一个控件背后的统计原理。建议配合[附录 A 参数速查表](#附录-a-参数速查表)对照阅读。

### 2.1 算法原理与目标函数

设有 $n$ 个对象，每个对象用 $p$ 个变量描述，记为 $x_i=(x_{i1},\dots,x_{ip})$。要把它们分成 $K$ 个簇 $C_1,\dots,C_K$，K-means 的目标是**最小化簇内平方和（WCSS，Within-Cluster Sum of Squares）**：

$$J=\text{WCSS}=\sum_{k=1}^{K}\sum_{x_i\in C_k}\lVert x_i-\mu_k\rVert^2,\qquad \mu_k=\frac{1}{|C_k|}\sum_{x_i\in C_k}x_i$$

其中 $\mu_k$ 是第 $k$ 个簇的**质心（centroid）**，即该簇所有点在每个变量上的均值。

**算法流程**（理解这四步就够了）：

1. **确定 K 与初始质心**：先选定簇数 $K$，并给出 $K$ 个初始中心（见 2.4）；
2. **分配（Assignment）**：把每个对象分配给**距离最近**的那个质心；
3. **更新（Update）**：对每个簇重新计算质心（取均值）；
4. **迭代**：重复 2–3，直到"分配结果不再变化"或达到最大迭代次数（**收敛**）。

> **关键直觉**：K-means 是在"分配"与"更新"之间反复横跳，每一步都让 WCSS 不增，所以它**一定会停下来**——但停下来的位置可能只是**局部最优**，这正是"起始值"很重要的原因。

### 2.2 距离度量

K-means 默认使用**欧氏距离**（Euclidean distance）：

$$d(x,y)=\sqrt{\sum_{j=1}^{p}(x_j-y_j)^2}$$

平台的 K-means 模块使用欧氏距离。欧氏距离对**量纲**和**量级**极其敏感，因此引出下一条。

### 2.3 为什么必须先标准化（"标准化聚类变量"）

举例：若同时用"年龄（30–80）"和"BMI（15–40）"聚类，年龄的波动范围远大于 BMI，**距离几乎完全由年龄决定**，BMI 等于白放。

**标准化（z 分数）**把每个变量变成"以标准差为单位"的分数：

$$z_{ij}=\frac{x_{ij}-\bar{x}_j}{s_j}$$

变换后每个变量的均值为 0、标准差为 1，**各变量在距离中权重相等**。

| 情形 | 是否需要勾选"标准化聚类变量" |
| --- | --- |
| 各变量量纲/量级差异大（如年龄 + 血糖） | **必须勾选** |
| 各变量量纲相近（如都是同一指标的两次测量） | 勾选与否结果差别很小（本指南案例就属于这种） |
| 希望某个变量"更重要" | 可用不标准化的方式变相加权，但需说明理由 |

### 2.4 起始值（初始中心）确定方法

因为 WCSS 可能有多个局部极小值，**初始质心的选择直接决定结果好坏**。平台提供 4 种方法（对应 `ClusterR` 的 4 种初始化器）：

| 选项 | 含义 | 特点 |
| --- | --- | --- |
| **kmeans++** | 按"离已有中心越远越容易被选中"的概率逐个挑初始中心 | **最常用、最推荐**；分散性好、稳定 |
| **random** | 从数据中随机挑 K 个点作初始中心 | 简单，但可能"撞车"（两个中心落在一起） |
| **quantile_init** | 按各变量的分位数组合出初始中心 | 让初始中心在数据范围内均匀铺开 |
| **optimal_init** | 先用一次层次聚类得到较好的初始中心 | 起点质量高，但计算更慢 |

**实测对比**（本指南案例数据，K=3，重复起始 25 次）：

| 起始值方法 | 各组样本量 | 簇内平方和 WCSSE | 轮廓系数 |
| --- | --- | --- | --- |
| kmeans++ | 370 / 851 / 852 | 1578.29 | 0.3458 |
| random | 866 / 852 / 355 | 1577.93 | 0.3474 |
| quantile_init | 863 / 880 / 330 | 1578.33 | 0.3508 |
| optimal_init | 866 / 852 / 355 | 1577.93 | 0.3474 |

**解读**：四种方法得到的解**几乎完全相同**（WCSSE 差异 < 0.05%，分组仅在边界上差十几个人）。这说明该数据结构清晰、结果**稳定**。

> **反过来**：如果换一种起始值方法，分组结果就面目全非，说明**数据本身没有清晰的簇结构**，此时任何"漂亮的分组"都值得怀疑（见 2.8）。

### 2.5 迭代与收敛

- **最大聚类次数**：界面上这个数字表示**尝试的簇数上限**（即 $K$ 从 1 一直试到该值，用于绘制"最优聚类数"曲线），**不是**迭代次数。
- **迭代次数**：每个 $K$ 下"分配—更新"的循环次数，由算法自行判断何时收敛（分配不再变化即停止）。
- **收敛**：正常收敛时，同一数据、同一 $K$、同一起始值方法应得到**可重复**的结果。若反复运行时分组剧烈跳动，说明结构不稳定。

### 2.6 如何确定聚类数 K（平台提供 5 个指标）

平台上"**最佳聚类绘图方法**"有 5 个下拉选项。它们各看什么、怎么判读，是这一节的重点。

#### ① 簇内平方和（WCSSE）——肘部法（Elbow）

- **定义**：每个簇内各点到该簇质心的距离平方和，再把所有簇加起来（即 2.1 的目标函数 $J$）。
- **判读**：$K$ 越大 WCSSE 一定越小（簇越多越"贴合"）。要找**下降幅度明显由陡变缓的"肘部"**。
- **优点**：直观。
- **缺点**：肘部常常**不唯一**，主观性强。

**本案例数值**（K=1→8）：

| K | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| WCSSE | 4144.00 | 2108.28 | 1578.31 | 1275.33 | 1037.99 | 905.56 | 804.95 | 721.25 |
| 相对下降 | — | **49%** | 25% | 19% | 19% | 13% | 11% | 10% |

> 1→2 组降幅最大（49%），2→3 为 25%，之后逐级趋缓 → **肘部大致落在 2–3 组之间**。

#### ② 轮廓系数（Silhouette）

- **定义**：对每个点 $i$，令 $a(i)$ 为它与**同簇**其他点的平均距离，$b(i)$ 为它与**最近的其他簇**所有点的平均距离，则

$$s(i)=\frac{b(i)-a(i)}{\max\{a(i),\,b(i)\}}$$

  所有点的 $s(i)$ 取平均即整体轮廓系数。取值范围 **−1 到 1**。
- **判读**：**越大越好**。经验阈值（Kaufman & Rousseeuw）：

| 轮廓系数 | 结构强度 |
| --- | --- |
| > 0.70 | 强结构 |
| 0.50 – 0.70 | 合理结构 |
| 0.25 – 0.50 | 结构较弱，可能是"人造"的 |
| < 0.25 | 基本没有实质结构 |

**本案例数值**：

| K | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 轮廓系数 | **0.4595** | 0.3458 | 0.3557 | 0.3252 | 0.3230 | 0.3056 | 0.3081 |

> 轮廓系数在 **K=2 最高**（0.4595），K=3 之后在 0.30–0.36 之间小幅波动。整体属于"**结构较弱**"——这提示数据本身的分群是渐变的，而不是天然割裂的几团。

#### ③ AIC / ④ BIC

- **定义**：在"拟合优度"与"模型复杂度"之间做权衡的信息准则，K-means 语境下近似为

$$\text{AIC} = n\ln(\text{WCSSE}/n) + 2pK,\qquad \text{BIC} = n\ln(\text{WCSSE}/n) + pK\ln n$$

- **判读**：一般**越小越好**。
- **重要提醒**：在 K-means 中 AIC/BIC 往往**随 K 单调下降**，给不出拐点。**看趋势即可，不要单独用它定 K。**

**本案例数值**：

| K | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AIC | 2116.28 | 1590.31 | 1291.33 | 1057.99 | 929.56 | 832.95 | 753.25 |
| BIC | 2138.83 | 1624.13 | 1336.43 | 1114.36 | 997.20 | 911.87 | 843.44 |

> 本例 AIC、BIC **一路下降、始终没有拐点**，因此**不能单独用来定 K**。

#### ⑤ 调整 R²（Adjusted R²）

- **定义**：簇间平方和占总平方和的比例（即"聚类能解释的变异百分比"），并按变量数做调整。
- **判读**：**越大越好**，但同样随 K 单调上升；应看**边际增益**——当"再加一个簇"带来的提升已很有限时，就该停下来。

**本案例数值**：

| K | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 调整 R² | 0.4910 | 0.6188 | 0.6918 | 0.7490 | 0.7809 | 0.8052 | 0.8254 |
| 相比上一步的增益 | — | **+0.128** | +0.073 | +0.057 | +0.032 | +0.024 | +0.020 |

> K=3 时调整 R² 达 0.619，较 K=2 有较大提升；之后每增加一组，增益迅速缩小（+0.073 → +0.057 → +0.032…），呈现明显的**边际效益递减**。

#### 综合判断 K 的方法（"四看"）

1. **看肘部**：WCSSE 的下降幅度在哪里由陡变缓（本例 2–3 组）；
2. **看轮廓系数**：是否处于"合理/强结构"区间，且哪一 K 最高（本例 K=2）；
3. **看边际增益**：调整 R² 每加一组的提升是否已经很小（本例 3 组之后明显递减）；
4. **看可解释性**：每个簇能否用临床语言讲清楚，且各簇样本量不至于过小（**这一条最重要**）。

> **不要只靠一个指标机械选择 K。** 指标之间常常"打架"（本例轮廓系数偏好 K=2，肘部落在 2–3，调整 R² 偏好更大 K），最终要靠**统计证据 + 临床意义 + 简约性**三方权衡。

### 2.7 聚类质量评价

除了"选 K"，还要评价**分得好不好**：

1. **整体轮廓系数**：见 2.6 ②，> 0.25 才算有初步结构，> 0.50 较理想。
2. **各簇的轮廓系数**：整体值可能被大簇"平均掉"，要看**每个簇**是否都在 0.25 以上。
   - 本案例 K=3 各簇轮廓系数：**簇1 = 0.2535、簇2 = 0.4295、簇3 = 0.3024** → 簇1 处于"结构较弱"的下限，说明高 TyG 这一簇与其他簇边界较模糊。
3. **各簇样本量**：避免出现占比 < 5% 的"小碎簇"。
4. **稳定性**：
   - 换**起始值方法**（2.4 实测四法结果几乎一致 → 稳定）；
   - 换**随机种子**多次重复，看分组是否一致；
   - 对数据**微小扰动**（如随机删除 5% 样本）后重聚，看类别是否保持。
5. **可解释性**：把各簇在各变量上的**均值**列出来，看是否呈现清晰的高低/方向模式。

### 2.8 常见陷阱

| 陷阱 | 后果 | 对策 |
| --- | --- | --- |
| 未标准化 + 量纲差异大 | 距离被"大数值变量"主导 | 勾选"标准化聚类变量" |
| 含分类变量 | 距离无意义 | 只用数值变量；分类变量改用 LCA |
| 未处理缺失 | 报错或结果错误 | 先插补或删除 |
| 未处理异常值 | 出现只含 1–2 个极端值的"假簇" | 先做异常值处理 |
| 盲目增加 K | 每组越来越小、越来越难解释 | 结合轮廓系数与边际增益 |
| 只看一次结果 | 可能落进局部最优 | 多起始值重复（模块默认重复起始），换方法复核 |
| 把聚类结果当作"真实分组" | 过度解读 | 聚类是**探索性**方法，需外部数据验证 |

---

## 第三章 平台 K-means 模块操作指南

### 3.1 模块总览

平台左侧菜单 **K-means 聚类分析模块** 下，围绕一条完整工作流组织：

| 顺序 | 页面 | 作用 |
| --- | --- | --- |
| 1 | **最优聚类选择** | 用 5 种指标绘制"聚类数—指标"曲线，帮你定 K |
| 2 | **聚类分析** | 按选定的 K 正式执行聚类 |
| 3 | **聚类可视化** | 把聚类结果画出来（散点图/均值折线图/密度图） |
| 4 | **分组差异性分析** | 按聚类分组，批量比较分类/百分比型变量（生成三线表） |
| 5 | **定量数据差异性分析** | 按聚类分组，批量比较定量变量（生成三线表） |
| 6 | **数据下载** | 把整理后的数据（含分组标签）导出 |

> **推荐操作顺序**：选定聚类变量 → 标准化 → 最优聚类选择（定 K）→ 聚类分析 → 聚类可视化 → 分组/定量差异性分析 → 数据下载。

### 3.2 最优聚类选择

**（1）基本参数**

| 控件 | 说明 | 建议 |
| --- | --- | --- |
| **聚类变量** | 选择参与聚类的变量（可多选） | 只用**数值型**、方向一致的变量 |
| **标准化聚类变量** | 勾选后对变量做 z 分数变换 | 量纲不同时**必须勾选** |
| **最大聚类次数** | 尝试的最大簇数（K 上限，默认 10） | 一般 8–10 足够 |
| **起始值确定方法** | kmeans++ / random / quantile_init / optimal_init | 默认 **kmeans++** |
| **最佳聚类绘图方法** | 簇内平方和 / 轮廓系数 / AIC / BIC / 调整R方 | 逐个画出来对比（可切换后重复绘制） |
| **绘制图像** | 出图 | 定 K 的核心步骤 |
| **重置参数** | 清空重来 | — |

**（2）主图设置**

| 控件 | 说明 |
| --- | --- |
| **最优聚类数辅助线** | 填入你倾向的 K，图上会画出竖线；留空则不显示 |
| **辅助线类型** | 虚线 / 实线 / 点线 / 点划线 |
| **辅助线颜色、辅助线粗细** | 竖线样式 |
| **折线颜色、散点颜色** | 曲线与拐点颜色 |
| **折线粗细、散点大小、散点透明度** | 图形粗细与通透度 |

**（3）坐标轴设置**

刻度大小、坐标标题大小、横坐标标题、纵坐标标题、X 轴左侧扩展、X 轴右侧扩展。

> 建议把坐标标题改成有含义的文字，如"聚类数 K""簇内平方和"，避免默认标题。

**（4）导出设置**

图像宽度（厘米）、图像高度（厘米）、分辨率（DPI，默认 300）、文件格式（**pdf / jpeg / png / tiff / svg / pptx**）、导出图形。

> **投稿建议**：矢量图优先 **pdf**（或用 **pptx** 便于二次编辑）；位图用 **tiff @300 DPI 以上**。

**（5）分析情况**

右侧显示当前图形；若参数不完整或变量未选，会给出提示。

### 3.3 聚类分析

**聚类分析设置**：

| 控件 | 说明 |
| --- | --- |
| **聚类变量** | 与上一步保持一致 |
| **标准化聚类变量** | 与上一步保持一致（**必须一致**，否则结果对不上） |
| **起始值确定方法** | 建议与定 K 时一致（默认 kmeans++） |
| **选择最优聚类数** | 填入你最终确定的 K |
| **执行聚类分析** | 正式运行 |

页面提示："**执行聚类分析后，请转到"聚类可视化"标签页进行绘图**"；运行完成后显示"**聚类分析已完成**"。

**聚类分析结果 → 聚类结果预览**：可预览每个对象的分组结果（ID 与所属簇）。

> **注意**：改变"聚类变量/标准化/起始值方法"里任何一项，都要**重新执行聚类分析**，否则后续可视化与差异性分析用的还是旧结果。

### 3.4 聚类可视化

**（1）绘图设置**

| 控件 | 说明 | 适用场景 |
| --- | --- | --- |
| **绘图类型** | **聚类散点图** | 2–3 个聚类变量时最直观；可看簇的分离程度 |
|  | **均值折线图** | 变量较多时，看每个簇在各变量上的**均值轮廓**（最常用于"给分型命名"） |
|  | **密度图** | 看某个变量在**各簇内的分布**是否分离 |
| **散点图变量（选择2个）** | 散点图的 X、Y 变量 | 绘图类型选散点图时出现 |
| **折线图变量** | 均值折线图的横向变量顺序 | 绘图类型选均值折线图时出现 |
| **密度图变量** | 密度图的变量 | 绘图类型选密度图时出现 |

**（2）主图设置**

- **选择配色方案**：Journal - Nejm / Journal - Lancet / Journal - Science / Default - Set1 / Default - Set2（期刊配色）
- **聚类颜色顺序**：调整簇的排列顺序，从而改变"颜色—簇"的对应与图例顺序
- **折线粗细、折线透明度、均值点大小、均值点透明度、密度图透明度**

**（3）坐标轴设置**：刻度大小、坐标标题大小、横纵坐标标题、X 轴左右扩展。

**（4）图例设置**：图例标题大小、图例大小、图例位置（右侧/正上方/正下方）、图例排列方式（水平/垂直）。

**（5）导出设置**：图像宽度/高度（厘米）、分辨率（DPI）、文件格式、**中文字体（有/无）**、导出聚类图形。

> **重要**：若图中标签含中文，请把"**中文字体**"设为"**有**"，否则中文会显示为方框或乱码。

### 3.5 分组差异性分析（分类/百分比型变量）

**作用**：把聚类分组当作"分组变量"，**批量**比较各组的分类变量（如性别、吸烟、是否患病），自动产出论文可直接用的**三线表**。

| 控件 | 说明 |
| --- | --- |
| **请选择分组变量（可选）** | 即聚类结果的分组变量（**必选**，通常就是聚类标签） |
| **请选择正态变量（可选）** | 各组比较用"均数 ± 标准差 + t 检验/方差分析"的定量变量 |
| **请选择偏态变量（可选）** | 各组比较用"中位数（四分位数）+ 秩和检验"的定量变量 |
| **请选择分类变量（卡方）** | 各组比较用"n(%) + 卡方检验"的分类变量 |
| **请选择百分比类型** | 百分比的基数/方向（按行/按列） |
| **统计缺失** | 是否在表中列出缺失值 |
| **请选择小数点位数** | 结果保留的小数位 |
| **是否需要计算 P 值** | Yes / No |
| **选择下载文档类型** | Excel / Word，点击"下载"导出 |
| 结果查看 | **1 分析结果 / 2 统计分析方法 / 3 结果描述（简单版 / 完整版） / 4 描述示例（英文）** |

> "**4 描述示例（英文）**"会给出可直接改写进论文的结果描述句式，非常实用。

### 3.6 定量数据批量差异性分析

**作用**：专门针对**定量指标**做分组比较，一次可以放进很多变量。

| 控件 | 说明 |
| --- | --- |
| **请选择定量检验变量** | 要比较的定量变量（可多选） |
| **该定量指标正态分布还是偏态分布** | **正态或近似正态** / **明显偏态或者异常分布** / **由软件开展正态性检验确定** |
| **请选择分组变量** | 被选中的变量将开展分组比较（**必选**） |
| **请选择小数点位数** | 结果小数位 |
| **是否需要计算 P 值** | Yes / No |
| **是否统计缺失** | Yes / No |
| **选择下载文档类型** | Excel / Word |
| 结果查看 | 1 分析结果 / 2 统计分析方法 / 3 结果描述（简单版 / 完整版） / 4 描述示例（英文） |

> **正态性怎么选**：不确定就选"**由软件开展正态性检验确定**"，让程序自动判断后选择 t 检验或秩和检验。

### 3.7 数据下载

| 控件 | 说明 |
| --- | --- |
| **数据格式** | **xlsx / csv / rds** |
| **保存数据** | 下载整理后的数据 |

> 导出的数据通常包含**原始变量 + 聚类分组标签**，可用于后续回归、生存分析或与轨迹模型联用。

---

## 第四章 完整实战案例

> 以下为**实际运行结果**：数据用与平台完全相同的计算引擎（R `ClusterR`）复现，参数与平台界面一致，供读者对照核对。

### 4.1 案例背景与数据

- **研究问题**：中国中老年人群的 **TyG 指数（甘油三酯-葡萄糖指数，反映胰岛素抵抗）**在 2011 与 2015 两次测量中，能否划分出不同的代谢表型？
- **数据**：`data/CHARLS_cluster.csv`（随本仓库提供），**2073 名**受访者，两个聚类变量：

| 变量 | 最小 | 下四分位 | 中位数 | 均值 | 上四分位 | 最大 | 标准差 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TyG_2011 | 6.625 | 8.195 | 8.551 | 8.646 | 8.976 | 12.353 | 0.645 |
| TyG_2015 | 7.212 | 8.221 | 8.596 | 8.686 | 9.068 | 11.328 | 0.630 |

- **无缺失**；两变量相关系数 **0.524**（中等正相关）。
- 两变量量纲相同（同一指标两次测量），标准化对结果影响很小——但仍按规范勾选"标准化聚类变量"。

### 4.2 操作步骤

1. **先登录平台**，进入 **K-means 聚类分析模块**。
2. **最优聚类选择**：
   - 聚类变量 = `TyG_2011`、`TyG_2015`；
   - 勾选 **标准化聚类变量**；
   - 最大聚类次数 = **8**；
   - 起始值确定方法 = **kmeans++**；
   - 最佳聚类绘图方法：依次切换 **簇内平方和 → 轮廓系数 → AIC → BIC → 调整R方**，各点一次"绘制图像"；
   - 在"最优聚类数辅助线"里填候选 K（如 3），便于对照。
3. **确定 K**：按"四看"准则综合判断（见 4.3）。
4. **聚类分析**：聚类变量、标准化、起始值方法与上一步一致，"选择最优聚类数"填 **3** → 点"**执行聚类分析**"。
5. **聚类可视化**：
   - 三张图分别看：**聚类散点图**（散点图变量选 TyG_2011 / TyG_2015）、**均值折线图**（折线图变量选两个 TyG）、**密度图**（分别看 TyG_2011、TyG_2015）；
   - 配色方案选 **Journal - Nejm**；图例位置"正下方"、水平排列；
   - 若坐标标题含中文，把"中文字体"设为**有**；
   - 按 300 DPI 导出 **pdf**（或 png 预览）。
6. **分组差异性分析 / 定量数据批量差异性分析**：分组变量 = 聚类分组，把研究中的基线变量放进去，导出 **Word/Excel** 三线表。
7. **数据下载**：格式选 **xlsx**，导出含分组标签的数据，用于后续分析。

### 4.3 结果：如何确定 K（真实数值）

**五个指标的完整结果**（K = 1…8）：

| K | 簇内平方和 WCSSE | 轮廓系数 | AIC | BIC | 调整 R² |
| --- | --- | --- | --- | --- | --- |
| 1 | 4144.00 | — | 4148.00 | 4159.27 | 0.0000 |
| 2 | 2108.28 | **0.4595** | 2116.28 | 2138.83 | 0.4910 |
| 3 | 1578.31 | 0.3458 | 1590.31 | 1624.13 | 0.6188 |
| 4 | 1275.33 | 0.3557 | 1291.33 | 1336.43 | 0.6918 |
| 5 | 1037.99 | 0.3252 | 1057.99 | 1114.36 | 0.7490 |
| 6 | 905.56 | 0.3230 | 929.56 | 997.20 | 0.7809 |
| 7 | 804.95 | 0.3056 | 832.95 | 911.87 | 0.8052 |
| 8 | 721.25 | 0.3081 | 753.25 | 843.44 | 0.8254 |

**按"四看"逐步判断**

1. **肘部（WCSSE）**：1→2 组下降 49%，2→3 组下降 25%，3→4 组降至 19%，之后逐级趋缓 → **肘部大致在 2–3 组**。
2. **轮廓系数**：**K=2 最高（0.4595）**；K=3 起降到 0.30–0.36 区间小幅波动 → 从"分类清晰度"看，2 组最好。
3. **边际增益（调整 R²）**：K=3 达 0.6188（较 K=2 提升 **+0.128**）；此后每组仅再提升 +0.073、+0.057、+0.032… **边际效益明显递减**。
4. **AIC / BIC**：**单调下降、无拐点**，本例**不能**用来定 K。
5. **可解释性**：3 组对应"**低 / 中 / 高**"三档 TyG 水平，各组占比 17.9% / 41.1% / 41.0%，均衡且临床易解读。

**结论**：综合肘部、边际增益与临床可解释性，**选择 K = 3 作为主分析模型**；同时把 **K = 2**（轮廓系数最优、分类最清晰）作为**敏感性分析**（见 4.5）。

> **诚实提示**：本例整体轮廓系数只有 0.35 左右，按 Kaufman 标准属于"结构较弱"。这说明 TyG 的人群分布是**渐变的**，而不是天然割裂的三团。因此结果应表述为"**探索性分型**"，不宜过度绝对化。

### 4.4 结果：K=3 的聚类结果（真实数值）

**各组规模**：

| 组别 | 样本量 | 占比 | 组内均值 TyG_2011 | 组内均值 TyG_2015 | 组内标准差（2011 / 2015） |
| --- | --- | --- | --- | --- | --- |
| 第 1 组（高） | 370 | 17.85% | **9.593** | **9.546** | 0.599 / 0.536 |
| 第 2 组（低） | 851 | 41.05% | **8.188** | **8.159** | 0.346 / 0.292 |
| 第 3 组（中） | 852 | 41.10% | **8.693** | **8.839** | 0.368 / 0.367 |

**整体质量指标**：

| 指标 | 数值 |
| --- | --- |
| 簇内平方和 WCSSE | 1578.29 |
| 聚类解释的变异比例 | 61.91% |
| 整体轮廓系数 | 0.3458 |
| 各簇轮廓系数 | 组1 = 0.2535；组2 = 0.4295；组3 = 0.3024 |
| 各簇内部平方和 | 585.05 / 427.14 / 566.10 |

**标准化的质心**（均值折线图上的取值，以标准差为单位）：

| 组别 | TyG_2011（z） | TyG_2015（z） |
| --- | --- | --- |
| 第 1 组 | +1.468 | +1.365 |
| 第 2 组 | −0.711 | −0.836 |
| 第 3 组 | +0.073 | +0.242 |

**临床命名建议**：

| 组别 | 占比 | 形态 | 建议命名 |
| --- | --- | --- | --- |
| 第 1 组 | 17.85% | 两次测量都明显偏高，且 2011→2015 略降 | **持续高 TyG 型（胰岛素抵抗高风险）** |
| 第 2 组 | 41.05% | 两次测量都偏低且稳定 | **持续低 TyG 型（代谢相对健康）** |
| 第 3 组 | 41.10% | 中等水平、略有上升 | **中等 TyG 上升型** |

> 命名原则：**结合临床意义**，用"水平 + 方向"两个维度描述，不要机械地只叫"第 1 类/第 2 类"。

### 4.5 敏感性分析

| 备选方案 | 轮廓系数 | 各组占比 | 解读 |
| --- | --- | --- | --- |
| **K = 2** | **0.4595** | 32.7% / 67.3% | 最稳健：低 TyG 组（均值 8.34 / 8.37）与高 TyG 组（9.27 / 9.34）；分类最清晰 |
| K = 3（**主分析**） | 0.3458 | 17.9% / 41.1% / 41.0% | 增加"中等"一档，临床信息更丰富 |
| K = 4 | 0.3557 | 12.2% / 28.6% / 37.7% / 21.6% | 可进一步区分**变化方向**：出现"低起升高"组（8.61 → 9.27）与"中高下降"组（8.89 → 8.51） |

**稳定性检验**（K=3，四种起始值方法各重复 25 次起始）：

| 起始值方法 | 各组样本量 | WCSSE | 轮廓系数 |
| --- | --- | --- | --- |
| kmeans++ | 370 / 851 / 852 | 1578.29 | 0.3458 |
| random | 866 / 852 / 355 | 1577.93 | 0.3474 |
| quantile_init | 863 / 880 / 330 | 1578.33 | 0.3508 |
| optimal_init | 866 / 852 / 355 | 1577.93 | 0.3474 |

四种方法结果**高度一致**（WCSSE 差异 < 0.05%），说明该聚类结果**稳定、可重复**。

### 4.6 后续：三线表与关联分析

- 用"**分组差异性分析**"和"**定量数据批量差异性分析**"，把基线变量（年龄、性别、BMI、血压、血脂、生活方式、共病等）按三个聚类组比较，直接得到论文用三线表；
- 用"**数据下载**"导出带分组标签的数据，在 SPSS/Stata/R 中做**多因素回归**（以聚类组为自变量，或以某结局为因变量），分析"高 TyG 组"与不良结局的关联。

---

## 第五章 结果解读与论文写作

### 5.1 结果报告要点

1. **方法部分**：说明聚类方法（K-means）、聚类变量、是否标准化、起始值方法、候选 K 范围与**确定 K 的依据**（列出各 K 的 WCSSE / 轮廓系数 / 调整 R² 等）。
2. **分组结果**：每组样本量与占比；每组在各聚类变量上的**均值（±标准差）**。
3. **聚类质量**：报告整体与**各组的轮廓系数**（或组间分离度指标），说明分类的可靠程度。
4. **临床命名**：给出各组命名并说明依据。
5. **组间比较**：用三线表呈现基线特征差异及其检验方法。
6. **敏感性分析**：报告备选 K（如 K=2、K=4）下结论是否一致。
7. **局限性**：聚类为**探索性**方法，需外部数据或前瞻队列验证；类别边界可能随样本变化。

### 5.2 英文结果描述模板（示例）

> Based on the within-cluster sum of squares (elbow), silhouette coefficient and adjusted R², a three-cluster solution was selected as the primary model. Cluster 1 (n = 370, 17.9%) showed persistently high TyG index (9.59 in 2011 and 9.55 in 2015); cluster 2 (n = 851, 41.1%) showed persistently low TyG index; and cluster 3 (n = 852, 41.1%) showed an intermediate TyG level with a slight increase over time. The overall silhouette coefficient was 0.35, indicating a weak but acceptable cluster structure.

### 5.3 图表规范建议

| 图形 | 建议 |
| --- | --- |
| 最优聚类数曲线 | 横轴写"聚类数 K"，纵轴写指标全称；标出所选 K 的辅助线 |
| 聚类散点图 | 只用 2–3 个变量；标注质心；配色用期刊配色（NEJM/Lancet/JCO） |
| 均值折线图 | 横轴为变量顺序，纵轴为标准化均值；适合给分型命名 |
| 密度图 | 用于说明某指标在各组的分布重叠程度 |
| 导出格式 | 投稿用 **pdf** 矢量图；位图 **tiff ≥ 300 DPI** |
| 中文字体 | 图中含中文时务必把"中文字体"设为**有** |

---

## 第六章 常见问题 FAQ

**Q1：聚类变量可以包含分类变量吗？**
不可以直接用。K-means 依赖欧氏距离，分类变量（文字或名义数字）没有合理的距离。若有分类变量，可考虑潜在类别分析（LCA）。

**Q2：一定要标准化吗？**
量纲不同的变量**必须**标准化；量纲相同（如同一指标多次测量）时可勾可不勾，结果差别很小。

**Q3：K 到底怎么定？**
先用"最优聚类数"页把 5 个指标都画出来，再按"四看"（肘部、轮廓系数、边际增益、可解释性）综合判断。切忌只看 AIC/BIC（在 K-means 里它们通常单调下降）。

**Q4：轮廓系数只有 0.3 左右，还能用吗？**
0.25–0.50 属于"结构较弱"，说明数据的分群是渐变的。可以报告，但应表述为**探索性分型**，并说明局限；同时做敏感性分析（换 K、换起始值方法）。

**Q5：为什么换一种起始值方法，分组结果变了？**
说明数据可能没有清晰结构，结果不稳定。此时应用 **kmeans++** 并**多次重复起始**（平台默认会重复），或降低 K。

**Q6：图上中文变成方框了？**
在"聚类可视化 → 导出设置 → **中文字体**"里选择"**有**"。

**Q7：改了聚类变量后图没变？**
必须**回到"聚类分析"页重新执行聚类分析**，再回"聚类可视化"绘图。可视化用的是上一次的执行结果。

**Q8：能出论文用的三线表吗？**
可以。"分组差异性分析"与"定量数据批量差异性分析"都能一键生成，并可导出 **Word/Excel**；还附带"统计分析方法""结果描述""英文描述示例"，可直接改写进论文。

**Q9：聚类结果怎么和结局做关联分析？**
用"数据下载"导出含分组标签的数据，在统计软件里把分组作为自变量（或分组后比较结局）做回归/生存分析。建议以**人数最多或风险最低**的一组为参照。

**Q10：聚类分析和轨迹模型能一起用吗？**
可以，但要注意数据类型不同：聚类用于**横截面**变量分型，轨迹模型用于**纵向**变化分组。参见本仓库的[轨迹模型 GMM 指南](./轨迹模型GMM入门与Shiny操作指南.md)。

---

## 附录 A 参数速查表

| 界面参数 | 位置 | 理论含义 | 推荐取值 |
| --- | --- | --- | --- |
| 聚类变量 | 最优聚类选择 / 聚类分析 | 参与聚类计算的数值变量 | 数值型、方向一致、少而精（2–8 个） |
| 标准化聚类变量 | 最优聚类选择 / 聚类分析 | z 分数变换，使权重相等 | 量纲不同时**必须勾选**；两处**必须一致** |
| 最大聚类次数 | 最优聚类选择 | 尝试的 K 上限 | 8–10 |
| 起始值确定方法 | 最优聚类选择 / 聚类分析 | 初始质心的生成方式 | **kmeans++** |
| 最佳聚类绘图方法 | 最优聚类选择 | 用哪个指标画"K—指标"曲线 | 5 个都看一遍 |
| 最优聚类数辅助线 | 主图设置 | 标出候选 K 的竖线 | 填你倾向的 K |
| 选择最优聚类数 | 聚类分析 | 最终确定的 K | 按"四看"准则综合判断 |
| 绘图类型 | 聚类可视化 | 散点图 / 均值折线图 / 密度图 | 2 变量用散点图；多变量用均值折线图 |
| 散点图变量 / 折线图变量 / 密度图变量 | 聚类可视化 | 指定绘图所用变量 | 按绘图类型选择 |
| 配色方案 | 主图设置 | 期刊配色 | NEJM / Lancet / JCO |
| 聚类颜色顺序 | 主图设置 | 颜色与簇的映射顺序 | 按临床顺序（低→高）排列 |
| 中文字体 | 导出设置 | 是否启用中文字体 | 图中有中文时选"有" |
| 文件格式 / DPI / 宽高 | 导出设置 | 导出规格 | 投稿：pdf；位图 tiff ≥300 DPI |
| 分组变量 | 分组/定量差异性分析 | 分组依据 | 选聚类分组标签 |
| 正态 / 偏态变量 | 分组差异性分析 | 决定用 t 检验还是秩和检验 | 不确定时选"由软件开展正态性检验确定" |
| 小数点位数 / P 值 / 统计缺失 | 差异性分析 | 表格呈现 | 一般小数 2 位、算 P 值、统计缺失 |

---

## 附录 B 术语表

| 术语 | 英文 | 简要解释 |
| --- | --- | --- |
| 聚类分析 | Cluster Analysis | 无监督地把相似对象归为一类 |
| 簇 | Cluster | 聚类得到的组 |
| 质心 | Centroid | 簇内各变量均值构成的中心点 |
| 簇内平方和 | WCSS / WCSSE | 簇内各点到质心的距离平方和，K-means 的目标函数 |
| 肘部法 | Elbow Method | 找 WCSSE 下降由陡变缓的拐点来确定 K |
| 轮廓系数 | Silhouette Coefficient | 衡量点与其簇的贴合度（−1~1，越大越好） |
| 调整 R² | Adjusted R² | 聚类解释的变异比例（校正后） |
| AIC / BIC | — | 信息准则，K-means 中常单调下降，不宜单独定 K |
| 标准化 | Standardization | 转为 z 分数，消除量纲影响 |
| kmeans++ | — | 一种让初始质心尽量分散的初始化方法 |
| 局部最优 | Local Optimum | 优化停在非全局最优的解 |
| 收敛 | Convergence | 分配结果不再变化 |
| 敏感性分析 | Sensitivity Analysis | 改变关键设定看结论是否稳健 |
| 三线表 | Three-line Table | 论文常用的规范统计表 |

---

## 附录 C 推荐阅读与参考文献

理论经典：

- MacQueen, J. (1967). Some methods for classification and analysis of multivariate observations. *Proceedings of the Fifth Berkeley Symposium on Mathematical Statistics and Probability*, 1, 281–297.（K-means 原始文献）
- Hartigan, J. A., & Wong, M. A. (1979). Algorithm AS 136: A K-means clustering algorithm. *Journal of the Royal Statistical Society: Series C*, 28(1), 100–108.
- Arthur, D., & Vassilvitskii, S. (2007). k-means++: The advantages of careful seeding. *SODA '07*, 1027–1035.（kmeans++ 原始文献）
- Rousseeuw, P. J. (1987). Silhouettes: A graphical aid to the interpretation and validation of cluster analysis. *Journal of Computational and Applied Mathematics*, 20, 53–65.（轮廓系数）
- Kaufman, L., & Rousseeuw, P. J. (1990). *Finding Groups in Data: An Introduction to Cluster Analysis*. Wiley.
- Milligan, G. W., & Cooper, M. C. (1985). An examination of procedures for determining the number of clusters in a data set. *Psychometrika*, 50(2), 159–179.

软件与实现：

- Mouselimis, L. `ClusterR`: Gaussian Mixture Models, K-Means, Mini-Batch-Kmeans, K-Medoids and Affinity Propagation Clustering（R 包，本平台底层引擎）。
- Maechler, M., et al. `cluster`: Cluster Analysis Basics and Extensions（R 包，轮廓系数实现）。
- 郑老师团队. 风暴统计平台 · K-means 聚类分析模块使用教程.

---

> 本指南基于"风暴统计平台 · K-means 聚类分析模块（v4.0.5）"的界面与实现撰写，第四章数值已用与平台相同的计算引擎（R `ClusterR`）实测复现。软件更新后，个别控件名称或默认值可能变化，请以实际界面为准。统计方法部分为通用原理，具体聚类决策仍应结合数据、研究问题与临床知识综合判断。
