# 轨迹模型（GMM）入门与 Shiny 操作指南

> 面向医学生与临床研究者的从零到一实操手册
> 适用工具：轨迹分析工具 v2.1.0（GMM 轨迹模型模块，轨迹单独版）
> 开发框架：R Shiny + shinydashboard；核心统计引擎：`lcmm` 包（`hlme` 函数）

---

## 目录

- [第一章 认识轨迹模型](#第一章-认识轨迹模型)
- [第二章 统计学理论基础](#第二章-统计学理论基础)
- [第三章 数据准备理论](#第三章-数据准备理论)
- [第四章 Shiny 工具操作指南](#第四章-shiny-工具操作指南)
- [第五章 完整实战案例](#第五章-完整实战案例)
- [第六章 结果解读与论文写作](#第六章-结果解读与论文写作)
- [第七章 常见问题 FAQ](#第七章-常见问题-faq)
- [附录 A 参数速查表](#附录-a-参数速查表)
- [附录 B 术语表](#附录-b-术语表)
- [附录 C 推荐阅读与参考文献](#附录-c-推荐阅读与参考文献)

---

## 第一章 认识轨迹模型

### 1.1 一个临床问题引出轨迹模型

设想你随访了 2000 名老年人，在 2011、2013、2015、2018、2020 年各测量一次"虚弱指数（Frailty Index）"。你会发现：

- 有些人一开始就很高、并且持续升高；
- 有些人维持在低位；
- 还有些人先低后高、或先高后低。

如果只看**平均值**，你会得到一条平滑的曲线，把所有这些差异全部抹平；如果对每个人单独建模，又会得到 2000 条杂乱无章的曲线，无法总结。

**轨迹模型（Trajectory Model）解决的核心问题**：能否把"看起来千差万别"的个体，归纳为**少数几种典型的发展轨迹（组/类别）**，并回答：

1. 一共有几种典型轨迹？（几组）
2. 每种轨迹长什么样？（形状、起点、变化速度）
3. 每个人最可能属于哪一组？
4. 哪些因素能预测属于哪一组？不同组结局有何差异？

### 1.2 医学生为什么需要它

| 研究场景 | 轨迹变量的例子 |
| --- | --- |
| 慢病进展 | 血压、血糖、BMI、虚弱指数随时间变化 |
| 心理健康 | 抑郁评分（CES-D、PHQ-9）随访轨迹 |
| 康复/术后 | 疼痛评分、生活质量随时间恢复模式 |
| 危险因素 | 吸烟量、饮酒量、体力活动的长期变化 |
| 认知老化 | 认知功能评分（MMSE）衰退轨迹 |
| 早期预警 | 用早期测量识别"高危轨迹"，指导早期干预 |

一句话：**轨迹模型让你从"横断面比较组间差异"升级到"刻画随时间演变的异质性"。**

### 1.3 轨迹模型家族：别被术语绕晕

文献中你会看到一堆近似名词，它们同源但有细微差别：

| 名称 | 英文 | 随机效应 | 特点 |
| --- | --- | --- | --- |
| 组基轨迹模型 | GBTM (Group-Based Trajectory Model) / LCGA | **不含**个体随机效应（方差固定为 0） | Nagin 提出，只估计组的形状；计算快、类别分离度好 |
| 潜类别增长分析 | LCGA (Latent Class Growth Analysis) | 组内无个体差异 | 与 GBTM 基本等价，偏结构方程传统 |
| 增长混合模型 | GMM (Growth Mixture Model) | **含**个体随机效应 | 允许组内个体围绕轨迹波动，更贴近现实，也更易不收敛 |

本工具的核心引擎 `lcmm::hlme`（heterogeneous linear mixed model）同时支持两者，通过界面上的"**是否包含随机效应**"开关切换：

- 取消勾选 → 近似 GBTM / LCGA（组内无随机波动）；
- 勾选 → GMM（组内有个体差异）。

工具在首页与菜单中统一使用"**潜类别增长混合模型（GMM）**"这一名称，但其能力覆盖上述两类模型。

### 1.4 核心思想：异质性 + 潜类别

轨迹模型建立在两个支柱上：

1. **潜变量（Latent Class）**：每个人背后有一个看不见的"类别标签"，它决定了这个人的轨迹形状。我们无法直接观测到类别，只能通过观测数据推断。
2. **异质性（Heterogeneity）**：总体不是同质的，而是由若干个同质的子群体"混合"而成——这正是"混合模型（Mixture Model）"名字的来源。

### 1.5 与其他方法的区别

| 方法 | 关注点 | 与轨迹模型的区别 |
| --- | --- | --- |
| 重复测量方差分析 | 组间均值随时间差异 | 需预设分组，无法探索未知亚群 |
| 混合效应模型（线性/非线性） | 总体平均变化 + 个体随机效应 | 假设**单一总体**，不识别多轨迹亚群 |
| 增长曲线模型（GCM/SEM） | 单一群体的变化形状 | 同样假设单一人群 |
| 聚类分析（横截面） | 同一时点的相似性 | 不含"时间"维度，不刻画变化 |
| **轨迹模型（GMM/GBTM）** | **同时刻画"变化"与"亚群"** | 兼具纵向与异质性视角 |

> 记忆诀窍：**混合效应模型回答"大家平均怎么变"，轨迹模型回答"大家是怎么以不同方式变的"。**

---

## 第二章 统计学理论基础

本章解释界面上每一个参数背后的统计原理。建议配合[附录 A 参数速查表](#附录-a-参数速查表)对照阅读。

### 2.1 模型的基本形式

对个体 $i$（$i=1,\dots,N$）在时间点 $t_{ij}$ 的观测值 $y_{ij}$，若其属于潜类别 $k$（$k=1,\dots,K$），则：

$$
y_{ij} \mid c_i = k \;=\; \beta_{0k} + \beta_{1k} t_{ij} + \beta_{2k} t_{ij}^2 + \beta_{3k} t_{ij}^3 \;+\; b_i \;+\; \varepsilon_{ij}
$$

各项含义：

- $\beta_{0k},\beta_{1k},\dots$：第 $k$ 类的**固定效应**，即该类轨迹的形状参数（截距、线性斜率、二次、三次项）。
- $b_i \sim N(0,\sigma_b^2)$：**个体随机效应**（如随机截距、随机斜率），刻画个体围绕所属类别轨迹的波动；若不勾选随机效应，则 $b_i\equiv 0$。
- $\varepsilon_{ij} \sim N(0,\sigma_\varepsilon^2)$：**残差**（测量误差）。
- $c_i$：个体 $i$ 的潜类别，属于第 $k$ 类的概率由多项 Logit 模型给出：

$$
P(c_i=k)=\frac{e^{\theta_k}}{\sum_{j=1}^{K} e^{\theta_j}},\qquad \sum_{k=1}^{K}P(c_i=k)=1
$$

**关键理解**：类别的概率不是外部给定的，而是与轨迹形状参数**一起、通过最大似然方法联合估计**出来的。

### 2.2 固定效应 vs 随机效应（界面的"高级参数"）

- **固定效应（Fixed Effects）**：全样本共享的、需要估计的回归系数，即轨迹的"骨架"。界面"包含固定效应"默认勾选，一般保持勾选。
- **随机效应（Random Effects）**：个体偏离群体轨迹的程度，是 GMM 与 GBTM 的分水岭。
  - **个体截距（`~1`）**：允许每个人有自己的起点高度；
  - **个体截距 + 时间斜率（`~1 + time`）**：还允许每个人有自己的变化速度。

> 实践建议：**先不加随机效应（GBTM）跑通、定下组数，再逐步加入随机效应做敏感性分析。** 加入随机效应会显著增加参数数量与不收敛风险。

### 2.3 多项式时间函数（界面的"多项式阶次"）

时间函数决定轨迹的"形状自由度"：

| 阶次 | 数学形式 | 轨迹形状 | 适用 |
| --- | --- | --- | --- |
| 1 阶（线性） | $\beta_0+\beta_1 t$ | 直线（单调上升/下降） | 最常见、最稳健，**优先尝试** |
| 2 阶（二次） | $\beta_0+\beta_1 t+\beta_2 t^2$ | 抛物线（可先升后降/先降后升） | 存在加速/减速或拐点 |
| 3 阶（三次） | 加 $\beta_3 t^3$ | S 形、双拐点 | 形状复杂，需大样本，易过拟合 |

**选择原则**：

1. **能低不高**——优先线性；只有当线性明显拟合不足（残差图、BIC 明显更差）时才升阶。
2. 阶次越高，需要估计的参数越多（每类多 1 个），越容易不收敛、越需要更多时间点。
3. 时间点少时（如仅 3 次随访）一般最多用二次；只有 2 个时间点时只能用线性。
4. 同一次建模中，不同类别**可以**共用同一阶次（本工具即如此设定）。

### 2.4 极大似然估计与 EM 算法

轨迹模型的参数估计采用**最大似然估计（MLE）**。因为类别是"隐变量"，直接优化很困难，常用 **EM 算法（Expectation–Maximization）** 交替进行：

- **E 步**：在给定当前参数下，计算每个个体属于各类的**后验概率**；
- **M 步**：按后验概率加权，更新轨迹形状参数与类别概率。

`lcmm` 实际使用基于 Marquardt 算法的准牛顿法直接优化边际似然（而非传统 EM），但思想相通。界面上的"**最大迭代次数**"就是控制这一优化过程允许迭代的上限（默认 100，范围 50–500）。

### 2.5 起始值问题：为什么会有"本地极大"（重要！）

似然函数是**多峰的（multimodal）**，优化结果高度依赖**起始值**。若起始值不好，你会得到一个"看起来收敛但其实是局部最优"的解。这是轨迹模型最容易踩坑的地方。

本工具提供两种起始值策略（界面"起始值确定方法"）：

#### ① 单类别模型法（single_class，默认）

先用全样本拟合 **1 组模型**（普通混合效应/增长模型），再以其参数为起点，配合 `lcmm` 内部自动生成的微扰启动多组模型。

- **优点**：快、稳定，多数情况够用。
- **缺点**：若真实类别结构偏离总体平均，仍可能落入局部最优。

#### ② 网格搜索法（grid_search）

在参数空间内**随机抽取多组起始值**，各拟合一遍，取对数似然最大者作为最终起点。

- 界面参数：**网格搜索重复次数**（默认 50，10–200）、**网格搜索最大迭代次数**（默认 20，5–100）。
- **优点**：更可能找到全局最优，结果更可信。
- **缺点**：计算时间成倍增加（重复次数 × 单次耗时）。

> **最佳实践**：先用"单类别模型法"快速探索组数；对最终候选模型（如 3 组），切换"网格搜索法"重复若干次，若对数似然/分组结果稳定，说明找到了可靠的全局解。

### 2.6 后验概率与个体归类

对每个个体，模型给出其属于 $K$ 个类别的**后验概率**（软件导出为 `Probability_Group1, ...`）。个体被归入**后验概率最大**的类别（"最大后验概率归类法"）。

- 后验概率越接近 1（或 0），归类越确定；
- 若某人各类概率都在 0.4–0.6 之间，说明归类不确定，慎用。

这一概念直接对应界面模型比较表中的 **AVE（平均后验概率）**（见 2.7）。

### 2.7 模型选择指标详解（界面"模型比较表"）

运行模型后，工具会给出 1 组、2 组、…直到"最大轨迹组数"的对比表格。各列含义如下：

| 表格列 | 含义 | 判读方向 |
| --- | --- | --- |
| 组数 G | 模型包含的轨迹类别数 | — |
| 对数似然值 loglik | 模型拟合优度的核心量 | **越大越好**（越接近 0） |
| 收敛 conv | 是否收敛（是/否） | 必须为"是"，否则结果不可用 |
| 参数数量 npm | 估计的参数个数 | 用于惩罚复杂度 |
| AIC | 赤池信息准则 | **越小越好** |
| BIC | 贝叶斯信息准则 | **越小越好**（最常用、惩罚最重） |
| SABIC | 样本量调整 BIC | **越小越好**（大样本时更稳健） |
| Entropy | 分类熵 | **越接近 1 越好**，> 0.8 表示分类清晰 |
| 组 k 比例(%) | 第 k 类人数占比 | 各类不宜过小（通常 ≥ 5%） |
| LMR p 值 | 似然比检验 p 值 | **< 0.05 支持更多的组**（见下） |
| AVE-组 k | 第 k 类平均后验概率 | **≥ 0.70** 表示归类可靠 |

#### 指标原理补充

- **信息准则（AIC/BIC/SABIC）**：在拟合优度与模型复杂度之间做权衡。公式近似为 $\text{BIC} = -2\log L + p\ln N$（$p$ 为参数数）。BIC 惩罚最重，通常**推荐以 BIC 为主，SABIC、entropy、类别比例、解释性为辅**。
- **Entropy（分类熵）**：衡量分类的"确定性"。经验标准：> 0.8 优，0.6–0.8 尚可，< 0.6 说明类别重叠严重。
- **LMR 检验（Lo–Mendell–Rubin 似然比检验）**：比较 $K$ 组与 $K-1$ 组模型（本工具对相邻组数两两比较），程序内部调用 `tidyLPA::calc_lrt`。**p < 0.05** 表示"$K$ 组显著优于 $K-1$ 组"；p ≥ 0.05 则倾向保留更少组。
- **AVE（平均后验概率）**：每类内成员归于该类的平均后验概率。**≥ 0.7**（更严格用 ≥ 0.8）是常用的分类质量门槛。

#### 组数决策的"五看"准则（推荐流程）

1. **看信息准则**：BIC/SABIC 出现最小值或开始变平；
2. **看检验**：LMR p 值 < 0.05；
3. **看分类质量**：Entropy ≥ 0.8，各类 AVE ≥ 0.7；
4. **看类别比例**：每类占比不宜过小（一般 ≥ 5%，或至少要能解释）；
5. **看可解释性**：各组轨迹形状能否用临床语言讲清楚（**这条最重要**）。

> **不要只靠一个指标机械选择组数！** 最终"几组最好"是**统计指标 + 临床意义 + parsimony（简约性）**共同决定的。常见做法是选 3–5 组。

### 2.8 收敛、迭代与常见报错

| 现象 | 含义 | 处理 |
| --- | --- | --- |
| 收敛 = 否 | 优化未达到收敛标准 | 增大最大迭代次数；简化模型（降阶/去随机效应）；换起始值（网格搜索）；检查数据 |
| 某类比例 < 1% | 出现"空类"或极小类 | 减少最大组数；简化模型 |
| 对数似然出现 `-Inf` | 模型严重不当或数据问题 | 检查时间点、缺失、变量类型 |
| 结果每次不同 | 落入不同局部最优 | 固定起始值方法并增加网格搜索次数，取最稳定解 |

### 2.9 时间尺度变换（界面的"将年份数据重新赋值"）

当时间变量是**真实年份**（如 2011, 2013, 2015, 2018, 2020）时，其数值很大。模型的优化算法对自变量的**尺度**非常敏感：尺度失衡会让似然曲面变得"又长又窄"，收敛极慢，甚至根本达不到收敛标准。

勾选该选项后，工具会把年份**重新编码为 1, 2, 3, …**（保持顺序不变），绘图时再自动把刻度标签**还原为原始年份**。

**实测佐证（本工具内置示例数据）**

| 设置 | 1–5 组模型的收敛情况 |
| --- | --- |
| 不勾选（时间 = 2011–2020） | **全部 `conv=2`**（达到最大迭代仍未收敛），4/5 组还出现占比为 0 的"空类别" |
| 勾选（时间 = 1–5） | **全部 `conv=1`**（收敛），迭代只需 9–38 次 |

- 从数学上讲，线性变换不改变模型能达到的最优拟合（只是坐标平移与缩放）；
- 但从**数值计算**上讲，它会大幅改善优化的条件数，**直接影响能否收敛**；
- 当模型使用二次 / 三次项时影响更大（年份的平方、立方会变成天文数字）；
- **建议：只要时间跨度大（例如真实年份），一律勾选。**

> 这也解释了：模型"不收敛"未必是数据或模型设定出错，**换一个时间尺度往往就能解决**。

---

## 第三章 数据准备理论

### 3.1 数据格式：长格式（Long Format）

轨迹模型要求**长格式（纵向/堆叠）数据**：**每一行是一个"个体 × 时间点"的观测**。

示例（本工具自带的 `longdata_traje.csv`）：

| 受访者编码 | year | Time | 虚弱指数FI | WHTR | ABSI | BRI |
| --- | --- | --- | --- | --- | --- | --- |
| 010104101002 | 2011 | 2011 | 3.459821 | 0.436464 | 0.079958 | 2.244587 |
| 010104101002 | 2011 | 2013 | 7.700892 | 0.426593 | 0.078641 | 2.085327 |
| 010104101002 | 2011 | 2015 | 14.397322 | 0.420322 | 0.077011 | 1.986084 |
| 010104101002 | 2011 | 2018 | 10.602678 |  |  |  |
| 010104101002 | 2011 | 2020 |  |  |  |  |
| 010104102002 | 2011 | 2011 | 8.258928 | 0.505952 | 0.084519 | 3.471125 |
| … | … | … | … | … | … | … |

要点：

- **同一 ID 重复出现多行**（每个随访时间点一行）；
- 允许缺失（如某人某次未测量）——这是轨迹模型的一大优势，**不需要删除缺失个体**。

> 与之相对的**宽格式**是"每个 ID 一行、每个时间点一列"。本工具在导出阶段支持"长转宽"（见 4.6）。

### 3.2 三个核心变量角色

| 角色 | 界面字段 | 要求 | 例子 |
| --- | --- | --- | --- |
| ID 变量 | `ID变量` | 唯一标识个体，可重复出现 | 受访者编码 |
| 时间变量 | `时间变量` | 数值型，标记观测的时间顺序 | Time（2011,2013,…） |
| 轨迹变量（因变量） | `轨迹变量` | 数值型，随时间变化的结局 | 虚弱指数FI |

> **三者都必须设为"定量变量（数值型）"**，否则模型无法运行（界面也会提示）。

### 3.3 缺失值处理

- **允许缺失**：轨迹模型基于似然估计，直接使用所有可用观测（相当于 MAR 假设下的极大似然），**不必事先填补**。
- **但要注意缺失机制**：若缺失与结局强相关（MNAR），结果可能有偏。
- 界面的"**缺失值分析**"图可快速查看各变量缺失率；缺失率过高的变量（如 > 50%）或个体建议谨慎处理。
- 至少保证**每个个体有 1 个以上观测**；一般建议每人 ≥ 2–3 个时间点才能刻画"变化"。

### 3.4 样本量与设计建议

| 维度 | 经验建议 |
| --- | --- |
| 总样本量 | 越大越好；GBTM 常见数百至数千，GMM 需更大样本支撑随机效应 |
| 时间点数 | ≥ 3 才能识别曲线形状；4–5 次随访较理想 |
| 每类人数 | 一般 ≥ 5%（至少≥ 30 人左右较稳定） |
| 组数上限 | 不宜超过 5–6 组，除非样本极大 |

### 3.5 建模前数据质量清单（Checklist）

- [ ] 数据为长格式（每行 = 个体 × 时间点）
- [ ] ID、时间、轨迹变量均为**数值型**
- [ ] ID 无重复冲突，时间点有明确顺序
- [ ] 轨迹变量已明确方向（分越高代表越严重？越低越差？）
- [ ] 核心变量缺失情况已查看，无异常值（如身高 2000 这种）
- [ ] 时间点数量 ≥ 3（高阶模型需更多）
- [ ] 若时间为年份且拟用高阶模型，准备好勾选"年份重新赋值"

---

## 第四章 Shiny 工具操作指南

### 4.0 界面总览

左侧菜单栏结构如下：

```
首页 - 功能介绍
└── GMM轨迹模型模块
    ├── 数据准备
    ├── 变量定义
    ├── 轨迹模型设置
    └── 轨迹结果可视化
```

**请严格按照"数据准备 → 变量定义 → 模型设置 → 结果可视化"的顺序操作**，因为每一步都依赖前一步的结果。

---

### 4.1 首页 — 功能介绍

- 展示应用概述、GMM 模块说明、工作流程、适用场景、快速开始指南与技术信息。
- 首次使用建议通读一遍，建立整体印象。
- "技术信息"中列出了开发框架与依赖包（`lcmm`、`factoextra`、`ggplot2`、`ggsci`、`shiny`、`shinydashboard` 等）。

---

### 4.2 数据准备

**目标**：导入数据、检查质量、了解变量分布。

#### ① 数据上传（左上 Box）

- **选择数据文件**：支持 `.csv`、`.xlsx`、`.xls`、`.sav`（SPSS）、`.sas7bdat`（SAS）、`.txt`。
- **CSV/TXT 专属选项**（上传后自动出现）：
  - **分隔符**：逗号 / 分号 / 制表符 / 空格；
  - **引号**：无 / 双引号 / 单引号；
  - **包含表头**：是否第一行为变量名。
- **Excel 专属选项**：
  - **工作表编号**（默认 1）；
  - **数据范围**（如 `A1:E100`，留空表示整表）。
- **加载示例数据**：一键载入内置示例 `longdata_traje.csv`（虚弱指数随访数据），**推荐初学者先用它练手**。

> 编码提示：工具会自动尝试 UTF-8，失败时回退 GBK，基本无需手动处理中文乱码。

#### ② 数据预览（右上 Box，三个标签页）

- **数据表**：交互式表格浏览，支持复制/导出（可翻页、横向滚动），底部有"下载数据"按钮。
- **数据摘要**：数据维度（行 × 列）、变量类型汇总、缺失值总数、完整观测数。
- **变量信息**：每个变量的类型、缺失数/率、唯一值数、最小值、最大值、均值、标准差。

#### ③ 数据质量检查（下方 Box）

- **数据概览**：观测数、变量数、缺失值、完整行、数据大小。
- **缺失值分析**：柱状图展示各变量缺失率（按从高到低排序），快速识别问题变量。

> **操作要点**：进入下一步前，务必先确认"轨迹变量、ID、时间"三列存在且大致合理。

---

### 4.3 变量定义

**目标**：为每个变量确认名称与类型，这是保证模型能运行的关键一步。

#### 界面构成

- 每个变量一行，包含两个控件：
  - **变量名**（可修改，仅用于显示）；
  - **变量类型**，三选一：
    - `分类变量（字符型）`——文本标签；
    - `分类变量（数值型）`——用数字编码的分类；
    - `定量变量（数值型）`——连续数值。
- **确认变量类型**按钮：点击后**应用**类型转换（如把字符数字转为数值）。
- 右侧**变量信息汇总**表：实时显示每个变量的类型、缺失率、唯一值数、最小值/最大值/均值/标准差。

#### 类型推断规则（工具默认）

- `factor` / `character` → 分类变量（字符型）；
- 数值型且**唯一值 ≤ 10** → 分类变量（数值型）；
- 其他数值型 → 定量变量（数值型）。

#### 操作要点（重要）

1. **必须把 ID 变量、时间变量、轨迹变量都设为"定量变量（数值型）"**。
2. 若某数值变量被自动识别为"分类"，需手动改回"定量"。
3. 修改后**务必点击"确认变量类型"**，否则后续模型无法运行（会提示"请在变量定义界面确认变量类型后再运行模型"）。
4. 提一句：这里的类型只影响后续进入模型时的数据类型处理，不会改变你的原始数据文件。

---

### 4.4 轨迹模型设置

**目标**：配置并运行轨迹模型。界面分三块参数区 + 一块操作区。

#### ① 基本参数（左上）

| 控件 | 说明 | 建议 |
| --- | --- | --- |
| **ID变量** | 选择个体标识列 | 必选 |
| **时间变量** | 选择时间列 | 必选 |
| **选择特定时间点进行建模** | 勾选后可只保留部分时间点建模 | 默认不勾（用全部时间点） |
| **时间点列表** | 勾选后可用的时间点（至少选 2 个） | 勾选"选择特定时间点"后出现 |
| **将年份数据重新赋值** | 把年份转为 1,2,3…（改善收敛） | 时间为年份且用高阶项时**建议勾选** |
| **轨迹变量** | 选择结局/因变量列 | 必选 |

#### ② 模型参数（中上）

| 控件 | 范围 | 说明 |
| --- | --- | --- |
| **多项式阶次** | 1–3 | 轨迹形状自由度（1=线性, 2=二次, 3=三次） |
| **最大轨迹组数** | 2–10 | 模型**最多**尝试到几组（组数越大计算越久） |
| **最大迭代次数** | 50–500 | 优化迭代上限（默认 100） |
| **自动运行 2~n 组模型** | 勾选/取消 | 勾选则依次运行 2、3、…直到最大组数；取消则只运行"最大轨迹组数"一个模型 |

> 无论是否勾选，工具都会**先运行 1 组模型**作为初始值，并把它一起放进比较表。

#### ③ 高级参数（右上）

| 控件 | 选项 | 说明 |
| --- | --- | --- |
| **起始值确定方法** | 单类别模型法 / 网格搜索法 | 见 2.5 |
| **网格搜索重复次数** | 10–200（默认 50） | 仅网格搜索法可设 |
| **网格搜索最大迭代次数** | 5–100（默认 20） | 仅网格搜索法可设 |
| **是否包含随机效应** | 勾选/取消 | 勾选 → GMM；取消 → GBTM/LCGA |
| **随机效应类型** | 个体截距 / 个体截距+时间斜率 | 勾选随机效应后出现 |
| **包含固定效应** | 默认勾选 | 一般保持勾选 |

#### ④ 模型操作（底部）

- **运行轨迹模型**：开始拟合（会弹出进度提示，运行期间请耐心等待）。
- **重置参数**：清空所有模型与参数，回到初始状态。
- **模型状态**：显示当前所选模型的组数、随机效应、起始值方法、时间点、年份变换、观测数、个体数、收敛状态、迭代次数。
- **模型比较表**：G / loglik / conv / npm / AIC / BIC / SABIC / Entropy / 各组比例 / LMR p 值 / AVE。**这是选择组数的主要依据**（判读标准见 2.7）。

#### 运行流程建议

1. 先**线性 + 不勾随机效应**，勾选"自动运行 2~n 组"，组数设到 5，快速跑通；
2. 依据比较表"五看"准则初选组数；
3. 对候选组数**加入随机效应**（或改二次项）做敏感性分析；
4. 用**网格搜索法**复核最终模型的稳定性。

---

### 4.5 轨迹结果可视化

顶部有两个子标签页：**"模型选择与绘图"** 与 **"导出轨迹模型结果"**。

#### 4.5.1 模型选择与绘图

**左栏（设置面板）**

**① 模型选择**
- **选择轨迹模型**：下拉选择已运行的某个模型（显示为"多项式X阶-N组-起始值方法"）；
- **执行轨迹预测**：点击后计算预测轨迹（含置信区间）；**必须先点此按钮才会出图**；
- **模型信息**：显示所选模型的基本摘要。

**② 坐标轴设置**（可折叠）
- **配色方案**：Journal - NEJM / Journal - Lancet / Journal - Science(JCO) / Default - Set1 / Default - Set2（发表常用期刊配色）；
- **轨迹颜色顺序**：用"↑ 上移 / ↓ 下移"调整类别顺序，从而改变颜色映射与图例顺序（点"恢复默认顺序"还原）；
- **刻度大小**、**坐标标题大小**；
- **横坐标标题**（默认 `time`）、**纵坐标标题**（默认 `Value`）——**建议改成临床变量名**，如 "随访年份""虚弱指数"；
- **Y轴最小值 / 最大值**：统一量程，便于多图对比；
- **X轴左侧扩展 / 右侧扩展**：坐标轴两端留白比例。

**③ 图例设置**（可折叠）
- **图例大小**、**图例位置**（右侧 / 正上方 / 正下方）、**图例排列方式**（水平 / 垂直）。

**④ 导出设置**（可折叠）
- **图像宽度 (600–2000px)**、**图像高度 (400–1200px)**、**分辨率 DPI (72–600)**；
- **文件格式**：PNG / PDF / TIFF / PPT（矢量）；
  - **PDF** 为矢量、可无损缩放，投稿首选；
  - **PPT（矢量）** 依赖 `officer` + `rvg` 包，可直接把矢量图形插入幻灯片二次编辑（如后期改配色）；
  - TIFF 满足部分期刊对位图分辨率的要求。
- **导出预测轨迹图**：下载当前图形。

**右栏（绘图区）**
- 实时展示预测轨迹图：每条曲线为一个类别，**带半透明置信带**，图例为 Class1/Class2…。

> **预览与导出一致性**：工具让预览容器按导出宽高比渲染，尽量保证"所见即所得"。

#### 4.5.2 导出轨迹模型结果（用于后续分析）

**目的**：把每个个体的"轨迹分组 + 后验概率"合并回原始数据，供后续回归/生存分析使用。

**左栏**
- **选择导出文件类型**：CSV / Excel（.xlsx）；
- **数据格式转换 → 转换为宽格式数据**（勾选后）：
  - **请继续选择不转为宽数据变量**：默认除 ID、时间、因变量、year、`_status`、`_time`、`_differ`、`_随访类型` 之外的变量都会转为宽格式；在此列表中**取消勾选**可把某些变量排除在"转宽"之外（即保持原样）；
  - **预览宽数据**：生成并查看宽格式结果（不下载）。
- **导出轨迹模型结果**：下载最终数据文件。

**右栏：宽数据格式预览**
- 显示宽数据前 20 行、生成状态提示与描述（如"X 个 ID、Y 列、包含 Z 个时间点的 XX 测量值"）。

**导出内容包含**：

| 列组 | 说明 |
| --- | --- |
| ID | 个体标识 |
| `Trajectory_Group` | 该个体的轨迹类别（1,2,3…） |
| `Probability_Group1..K` | 属于各类的后验概率 |
| 原始协变量 | 合并回来的其他变量 |
| （宽格式时）`变量_时间` | 如 `虚弱指数FI_2013`，每个时间点一列 |

> **宽格式命名规则**：`变量名_时间点`，例如 `FI_2011`、`FI_2013`。宽格式便于在 SPSS/Stata/R 中做**以基线协变量预测类别归属**或**以类别为自变量分析结局**。

**后续分析提示**：用 `Trajectory_Group` 作为分类自变量（或结局）时，一般以**人数最多或风险最低**的组作为参照组。

---

## 第五章 完整实战案例

### 5.1 案例背景与数据

- **研究问题**：中国老年人**虚弱指数（FI）**在 2011–2020 年间的变化存在哪些典型轨迹？
- **数据**：工具内置示例 `longdata_traje.csv`（长格式）。
- **三个核心变量**：
  - ID = `受访者编码`
  - 时间 = `Time`（2011, 2013, 2015, 2018, 2020）
  - 轨迹变量 = `虚弱指数FI`

### 5.2 操作步骤（逐步）

1. **首页** → 浏览功能与流程。
2. **数据准备** → 点击"**加载示例数据**"。
   - 在"数据表"查看数据；"变量信息"确认 `受访者编码`、`Time`、`虚弱指数FI` 均为数值型；
   - "缺失值分析"查看 `WHTR/ABSI/BRI` 存在较多缺失（正常，因为模型只用 FI）。
3. **变量定义** → 确认 `受访者编码`、`Time`、`虚弱指数FI` 为"**定量变量（数值型）**" → 点击"**确认变量类型**"。
4. **轨迹模型设置**：
   - ID变量 = `受访者编码`；时间变量 = `Time`；轨迹变量 = `虚弱指数FI`；
   - **勾选"将年份数据重新赋值"（本案例的关键一步）**：时间取值是 2011–2020、数值尺度大。实测不勾选时 1–5 组模型全部 `conv=2`（未收敛）、4/5 组还出现空类别；勾选后全部 `conv=1`、迭代仅 9–38 次；
   - 多项式阶次 = **1**（先线性）；最大轨迹组数 = **5**；最大迭代次数 = **100**；
   - 起始值方法 = **单类别模型法**；随机效应 = **不勾选**（先 GBTM）；
   - 勾选"**自动运行 2~n 组模型**" → 点击"**运行轨迹模型**"。
5. **查看"模型比较表"**，按"五看"准则选组数。
6. **轨迹结果可视化** → "模型选择与绘图"：
   - 选择"多项式1阶-4组-单类别法" → 点击"**执行轨迹预测**"；
   - 横/纵坐标标题改为"随访时间""虚弱指数"；配色选 NEJM；图例放正下方；
   - 满意后按 300 DPI、PDF 导出。
7. **稳健性检查**（可选）：回到模型设置，加入随机效应或改为二次项，重复上述步骤对比。
8. **导出轨迹模型结果** → 勾选"转换为宽格式数据" → "预览宽数据"确认无误 → 导出。

### 5.3 结果解读（真实运行结果）

> 以下为按 5.2 参数在工具中实际运行、并与工具底层引擎（`lcmm::hlme`）逐步复现核对后完全一致的结果。
> 数据：400 名受访者、1999 条观测（`虚弱指数FI` 有 672 条缺失），时间点经"年份重赋值"后为 1–5。

**模型比较表**（线性、无随机效应、单类别模型法起始值、最大迭代 100）

| 组数 | 对数似然值 | 收敛 | 参数数量 | AIC | BIC | SABIC | Entropy | 组1% | 组2% | 组3% | 组4% | 组5% | LMR p | AVE-组1 | AVE-组2 | AVE-组3 | AVE-组4 | AVE-组5 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | -5029.000 | 是 | 3 | 10064.000 | 10075.959 | 10066.440 | 1.000 | 100.00 | | | | | — | 1.000 | | | | |
| 2 | -4803.159 | 是 | 6 | 9618.317 | 9642.236 | 9623.198 | 0.862 | 74.12 | 25.88 | | | | <0.001 | 0.972 | 0.929 | | | |
| 3 | -4737.276 | 是 | 9 | 9492.551 | 9528.429 | 9499.872 | 0.853 | 66.33 | 26.63 | 7.04 | | | <0.001 | 0.943 | 0.915 | 0.908 | | |
| **4** | **-4716.754** | **是** | **12** | **9457.509** | **9505.346** | **9467.269** | **0.828** | **62.31** | **25.13** | **6.03** | **6.53** | | **<0.001** | **0.946** | **0.815** | **0.780** | **0.908** | |
| 5 | -4693.751 | 是 | 15 | 9417.502 | 9477.299 | 9429.704 | 0.759 | 47.49 | 9.80 | 9.55 | 28.14 | 5.03 | <0.001 | 0.875 | 0.774 | 0.790 | 0.792 | 0.915 |

> 界面中"对数似然值 / AIC / BIC / SABIC / Entropy"按 3 位小数、类别比例按 3 位小数、AVE 按 5 位小数显示；上表为便于阅读做了四舍五入。表中空白表示该模型没有这个类别。

**按"五看"准则逐步判断**

1. **信息准则**：AIC、BIC、SABIC 随组数**单调下降、始终没有拐点**（BIC：10076 → 9642 → 9528 → 9505 → 9477）。这是 GMM 的典型现象——**单靠信息准则定不了组数**。
2. **LMR 检验**：1→2、2→3、3→4、4→5 的 p 值**均 < 0.001**，即每一步增加组数都"统计学显著"；该指标同样**给不出上界**。
3. **Entropy**：2 组最高（0.862），随后逐级下降；**5 组已降到 0.759**（< 0.80），分类开始变模糊。
4. **AVE**：2–4 组的绝大多数类别 AVE ≥ 0.78（4 组的"组3"为 0.780，略低于 0.80 门槛）；**5 组有 3 个类别的 AVE 只有 0.77–0.79**。
5. **类别比例与可解释性**：
   - 3 组：66.33 / 26.63 / 7.04
   - 4 组：62.31 / 25.13 / 6.03 / 6.53
   - 5 组：47.49 / 9.80 / 9.55 / 28.14 / **5.03（出现 <6% 的小类）**

**结论（综合判断）**

信息准则与 LMR 都在"鼓励"增加组数、给不出上界，因此要重点看**分类质量 + 类别规模 + 可解释性**：

- **3 组**：最简约、分类最清晰（各类 AVE ≥ 0.91），但可能把形态明显不同的亚群合并了；
- **4 组**：新增了一个极具临床价值的"低起—快速恶化"高危亚群；ΔBIC(3→4) = −23.1、LMR 显著、各类占比均 ≥ 6%、Entropy 0.828 尚可接受 → **综合平衡最佳**；
- **5 组**：BIC 虽最低，但 Entropy 掉到 0.759、出现 5.03% 的小类、多个类别 AVE < 0.80，分类可靠性明显下降。

→ 因此**选择 4 组作为主分析模型**，并把 3 组、5 组留作敏感性分析。

**四组轨迹的形态与临床命名**（数值为该组在四个随访年度的 FI 均值）

| 类别 | 占比 | FI 均值（2011 → 2013 → 2015 → 2018） | 形态 | 建议命名 |
| --- | --- | --- | --- | --- |
| 组1 | 62.31% | 7.95 → 9.42 → 10.37 → 11.75 | 低位、缓慢上升 | 低虚弱—缓慢上升型 |
| 组2 | 25.13% | 19.99 → 20.59 → 23.59 → 24.23 | 中等位、基本平稳 | 中等虚弱—平稳型 |
| 组3 | 6.03% | 10.53 → 16.28 → 33.13 → 41.00 | 起点偏低、后期急速攀升 | 低起—快速恶化型（**高危**） |
| 组4 | 6.53% | 34.21 → 38.96 → 41.41 → 42.86 | 高位、持续缓升 | 高虚弱—持续进展型 |

> **注意**：2020 年度的 `虚弱指数FI` 全部缺失，因此曲线实际只覆盖 2011、2013、2015、2018 四个时点。**组3** 虽然只占 6.03%，但"低起—急速恶化"具有很强的公共卫生意义，是早期干预的重点人群。

### 5.4 本案例的启示

- **"年份重赋值"不是可选项，而是关键**：本例不勾选时 1–5 组模型全部 `conv=2`（未收敛），勾选后全部 `conv=1`。遇到"收敛 = 否"，**先检查时间尺度**。
- **信息准则"单调下降"是陷阱**：AIC/BIC/SABIC 一路下降、LMR 一路显著，机械套用会得出"组数越多越好"的错误结论；必须结合 Entropy、AVE、类别规模与可解释性综合判断。
- **警惕小类别**：5 组中出现 5.03% 的小类，提示可能过度切分，应减少组数或做敏感性分析。
- **起始值敏感性**：若更换起始值方法后结果明显变化，说明存在局部最优，用网格搜索法（增大重复次数）复核（见 2.5）。
- **可复现**：使用内置示例数据、按 5.2 的参数原样操作即可复现上表；建议读者自行跑一遍核对数值。

---

## 第六章 结果解读与论文写作

### 6.1 结果报告要点（推荐清单）

1. **模型设定**：说明使用 GBTM 还是 GMM、时间函数阶次、是否有随机效应、起始值策略、迭代与收敛。
2. **组数确定**：报告 1–K 组模型的 BIC/SABIC/AIC、Entropy、LMR p 值、各组比例与 AVE，说明最终选择依据。
3. **轨迹描述**：用文字 + 图描述每类轨迹的形状、起点、变化趋势，并给出临床命名。
4. **分类质量**：报告 Entropy 与各类 AVE（≥ 0.7/0.8）。
5. **后续分析**：说明以哪一组为参照，如何把 `Trajectory_Group` 纳入后续回归（并提示分类不确定性可通过后验概率加权等方法处理）。

### 6.2 图表规范建议

- **配色**：优先期刊配色（NEJM/Lancet/JCO），同一研究保持一致；
- **坐标轴**：写具体变量名与单位，不要用默认 `time`/`Value`；
- **图例**：类别名用临床可读名称（如 "Low-stable"），若用 Class1/2 需在文中对应说明；
- **置信带**：GMM 建议展示 95% 置信带，体现不确定性；
- **导出**：投稿优先矢量格式（PDF/PPT），位图用 TIFF ≥ 300 DPI；
- **Y 轴量程**：多图对比时统一量程，避免"视觉误导"。

### 6.3 报告规范参考

轨迹分析的规范报告可参照 **GRoLTS 清单**（Guidelines for Reporting on Latent Trajectory Studies），涵盖是否报告组数依据、分类质量、缺失处理、模型稳定性等 16 条要求，是审稿人常看的要点。

---

## 第七章 常见问题 FAQ

**Q1：必须用随机效应吗？**
不必。先不加（GBTM）跑通并定组数，再逐步加入做敏感性分析。加了随机效应会显著增加参数与不收敛风险。

**Q2：可以只运行某一个组数吗？**
可以。取消"自动运行 2~n 组模型"，则只运行"最大轨迹组数"那一个模型（1 组模型仍会作为初始值运行）。

**Q3：模型一直不收敛怎么办？**
按顺序尝试：①增大最大迭代次数 → ②改用线性阶次 → ③勾选"年份重新赋值" → ④取消随机效应 → ⑤删除样本过少的时间点 → ⑥改用网格搜索法。仍不行则检查数据（异常值、时间点、变量类型）。

**Q4：时间点里有人数很少的随访波次，能剔除吗？**
可以。勾选"选择特定时间点进行建模"，只保留你需要的波次。

**Q5：为什么"执行轨迹预测"前不出图？**
绘图依赖预测结果，必须**先在"模型选择与绘图"里选定模型 → 点击"执行轨迹预测"**，才会渲染图形。

**Q6：改配色/标题后图没变？**
先确认已执行过预测；所有图形相关控件会在预测结果基础上即时刷新。若仍不动，重新点击"执行轨迹预测"。

**Q7："导出 PPT（矢量）"报错？**
该功能依赖 `officer` 和 `rvg` 两个包，缺失时不可用（不影响其他功能）。安装：`install.packages(c("officer","rvg"))`。

**Q8：导出的宽数据里，哪些变量被转了宽？**
除 ID、时间、因变量、`year`、`_status`、`_time`、`_differ`、`_随访类型` 外的变量默认转宽；你可在"请继续选择不转为宽数据变量"里取消勾选，逐一把变量排除出"转宽"列表。

**Q9：能否直接做"轨迹分组与结局的关联分析"？**
本工具负责**生成轨迹分组**；关联分析（回归、生存分析等）请在导出数据后于 SPSS/Stata/R 中完成。工具导出的 `Trajectory_Group` 与后验概率列即可直接使用。

**Q10：如何判断某个个体的归类是否可靠？**
看他的后验概率：最大概率越接近 1 越可靠；若各类概率接近（如都在 0.4–0.6），归类不确定，分析时应谨慎（可做敏感性分析）。

---

## 附录 A 参数速查表

将界面参数与理论概念一一对应，方便快速查阅。

| 界面参数 | 位置 | 理论含义 | 推荐取值 |
| --- | --- | --- | --- |
| 分隔符 / 引号 / 表头 / 工作表 / 数据范围 | 数据准备 | 数据读取选项 | 按文件实际 |
| 变量类型 | 变量定义 | 数据类型声明 | ID/时间/轨迹变量取"定量变量（数值型）" |
| ID变量 | 模型设置 | 个体标识 | 必选 |
| 时间变量 | 模型设置 | 时间轴 $t$ | 必选，数值型 |
| 选择特定时间点 | 模型设置 | 数据子集 | 默认全选 |
| 将年份数据重新赋值 | 模型设置 | 时间尺度变换（数值稳定） | 年份 + 高阶项时勾选 |
| 轨迹变量 | 模型设置 | 结局 $y$ | 必选，数值型 |
| 多项式阶次 | 模型参数 | 时间函数阶次（1/2/3） | 从 1 开始 |
| 最大轨迹组数 | 模型参数 | 类别数上限 $K_{max}$ | 3–5 |
| 最大迭代次数 | 模型参数 | 优化迭代上限 | 100（不收敛时增大） |
| 自动运行 2~n 组 | 模型参数 | 组数枚举 | 首次勾选 |
| 起始值确定方法 | 高级参数 | 解决局部最优 | 探索用"单类别"，复核用"网格搜索" |
| 网格搜索重复次数 | 高级参数 | 随机起点次数 | 50（复核时可增大） |
| 网格搜索最大迭代次数 | 高级参数 | 网格搜索迭代上限 | 20 |
| 是否包含随机效应 | 高级参数 | GMM vs GBTM | 先不勾，后期加 |
| 随机效应类型 | 高级参数 | 随机截距 / 随机截距+斜率 | 先截距 |
| 包含固定效应 | 高级参数 | 轨迹骨架 | 保持勾选 |
| 模型比较表 | 模型操作 | 组数选择依据 | 见 2.7"五看" |
| 配色方案 | 可视化 | 图形配色 | NEJM/Lancet |
| 轨迹颜色顺序 | 可视化 | 类别-颜色映射 | 按临床顺序 |
| Y轴最小/最大 | 可视化 | 量程统一 | 多图对齐 |
| 导出格式 | 可视化 | PNG/PDF/TIFF/PPT | 投稿用 PDF/TIFF |
| 宽格式转换 | 导出结果 | 长→宽数据整形 | 需做多变量分析时勾选 |

---

## 附录 B 术语表

| 术语 | 英文 | 简要解释 |
| --- | --- | --- |
| 轨迹模型 | Trajectory Model | 识别随时间变化的亚群轨迹 |
| 潜类别 | Latent Class | 不可直接观测的类别变量 |
| GBTM | Group-Based Trajectory Model | 不含随机效应的组基轨迹模型 |
| LCGA | Latent Class Growth Analysis | 潜类别增长分析（近似 GBTM） |
| GMM | Growth Mixture Model | 含随机效应的增长混合模型 |
| 固定效应 | Fixed Effect | 全样本共享的回归系数 |
| 随机效应 | Random Effect | 个体偏离群体的程度 |
| 多项式阶次 | Polynomial Degree | 时间函数次数（1/2/3） |
| 对数似然 | Log-likelihood | 拟合优度核心量 |
| AIC / BIC / SABIC | — | 信息准则，越小越好 |
| Entropy | 分类熵 | 分类清晰度，越接近 1 越好 |
| LMR | Lo–Mendell–Rubin LRT | 相邻组数似然比检验，p<0.05 支持更多组 |
| 后验概率 | Posterior Probability | 个体属于各类的概率 |
| AVE | Average Posterior Probability | 类内平均后验概率，≥0.7 为佳 |
| 收敛 | Convergence | 优化达到稳定解 |
| 长格式 / 宽格式 | Long / Wide Format | 每人多行 / 每人一行 |
| 网格搜索 | Grid Search | 多起点搜索全局最优 |
| 起始值 | Initial Values | 优化的起点，影响是否局部最优 |

---

## 附录 C 推荐阅读与参考文献

理论经典：

- Nagin, D. S. (2005/2014). *Group-Based Modeling of Development*. Harvard University Press.
- Lo, Y., Mendell, N. R., & Rubin, D. B. (2001). Testing the number of components in a normal mixture. *Biometrika*, 88(3), 767–778.
- Nylund, K. L., Asparouhov, T., & Muthén, B. O. (2007). Deciding on the number of classes in latent class analysis and growth mixture modeling. *Structural Equation Modeling*, 14(4), 535–569.
- Ram, N., & Grimm, K. J. (2009). Growth mixture modeling: A method for identifying differences in longitudinal change among unobserved groups. *International Journal of Behavioral Development*, 33(6), 565–576.
- van de Schoot, R., Sijbrandij, M., Winter, S. D., Depaoli, S., & Vermunt, J. K. (2017). The GRoLTS-Checklist: Guidelines for reporting on latent trajectory studies. *Structural Equation Modeling*, 24(3), 451–467.

软件与实现：

- Proust-Lima, C., Philipps, V., & Liquet, B. `lcmm`: Extended Mixed Models Using Latent Classes and Latent Processes（R 包，含 `hlme`）。
- Rosenberg, J., et al. `tidyLPA`: Tidy Latent Profile Analysis（R 包，本工具用于 LMR 检验）。
- Muthén, B., & Muthén, L. *Mplus User's Guide*（轨迹/混合模型权威软件手册，理论说明详尽）。

---

## 附录 D 运行环境与依赖（供部署/复现用）

- **框架**：R + Shiny + shinydashboard。
- **必需 R 包**：`shiny`、`shinydashboard`、`DT`、`readxl`、`haven`、`readr`、`dplyr`、`shinyjs`、`ggplot2`、`lcmm`、`ggsci`、`RColorBrewer`、`ClusterR`、`writexl`、`reshape2`、`colourpicker`、`tidyLPA`。
- **可选 R 包**：`officer`、`rvg`（仅"导出 PPT（矢量）"功能需要）。
- **安装**：

```r
install.packages(c("shiny","shinydashboard","DT","readxl","haven","readr",
                   "dplyr","shinyjs","ggplot2","lcmm","ggsci","RColorBrewer",
                   "ClusterR","writexl","reshape2","colourpicker","tidyLPA",
                   "officer","rvg"))
```

- **启动**：在应用目录下执行 `shiny::runApp()`。
- **上传大小上限**：100 MB（`options(shiny.maxRequestSize = 100 * 1024^2)`）。

---

> 本指南基于"轨迹分析工具 v2.1.0（轨迹单独版）"的界面与实现撰写。软件更新后，个别控件名称或默认值可能变化，请以实际界面为准。统计方法部分为通用原理，具体建模决策仍应结合数据、研究问题与临床知识综合判断。
